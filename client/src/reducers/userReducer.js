const defaultState = {
    currentUser: {},
    isAuth: false
}

export function userReducer(state = defaultState, action) {
    switch (action.type){


        default:
            return state
    }
}